#include "Pods-eppz!swizzler.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/eppz!swizzler" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/eppz!swizzler"
OTHER_LDFLAGS = -ObjC ${PODS_EPPZ_SWIZZLER_OTHER_LDFLAGS}
PODS_ROOT = ${SRCROOT}